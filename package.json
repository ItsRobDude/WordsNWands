{
  "name": "words-n-wands-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "content:generate:encounters": "node packages/content/src/generation/cli/generateEncounters.js",
    "lint": "node scripts/check-syntax-from-tsconfig.mjs",
    "test": "node scripts/run-tests-from-vitest-config.mjs",
    "typecheck": "node scripts/check-syntax-from-tsconfig.mjs",
    "build": "node scripts/check-syntax-from-tsconfig.mjs",
    "format": "echo 'No formatter configured yet'",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  }
}
